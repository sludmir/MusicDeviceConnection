# LiveSet Product Factory

See **[PROCESS.md](./PROCESS.md)** for the full guide (setup, commands, email approve, troubleshooting).

Quick start:

```bash
npm run factory:week -- --limit 2
```

Then approve the **3D model previews** in email → `npm run dump-products`.
