/**
 * Connection suggestions stub. Replace with real API when ready.
 */

export async function getConnectionSuggestions(devices) {
  if (!devices || !devices.length) return '';
  return 'Add your API integration in chatGPTService.js to get connection suggestions.';
}
